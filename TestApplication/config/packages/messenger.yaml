framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            #async: '%env(MESSENGER_TRANSPORT_DSN)%'
            async: doctrine://default
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        routing:
            App\Infrastructure\Bus\Message\Command\AsyncCommandInterface : async
            App\Infrastructure\Bus\Message\Command\SyncCommandInterface : sync
            App\Infrastructure\Bus\Message\Event\AsyncEventInterface : async
            App\Infrastructure\Bus\Message\Event\SyncEventInterface : sync

when@test:
    framework:
        messenger:
            transports:
                # replace with your transport name here (e.g., my_transport: 'in-memory://')
                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
                async: 'in-memory://'
